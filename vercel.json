{
  "builds": [
    {
      "src": "api/index.py",
      "use": "@vercel/python",
      "config": { "includeFiles": ["app/**", "static/**", "models/lookup.json.gz"] }
    }
  ],
  "routes": [{ "src": "/(.*)", "dest": "api/index.py" }]
}
